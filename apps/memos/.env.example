# Memos Local Environment Variables
# This file contains app-specific configuration for Memos.
# Global variables (TZ, DOMAIN) are inherited from the root .env file.

# No app-specific variables required for basic setup.
# Memos uses sensible defaults and stores data in /var/opt/memos
